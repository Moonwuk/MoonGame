// React Native mobile client — Stage 4 of docs/roadmap.md.
//
// Will host: the app shell, the WebSocket connection with graceful reconnect,
// the Skia star-map renderer (zoom / scroll / culling), diff application, and
// battle preview powered by @void/shared-core (the same simulation the server
// runs — docs/architecture.md §3).
//
// Intentionally empty until Stage 2 (action layer) is in place.
export {};
